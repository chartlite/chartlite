/**
 * chartlite React
 * React components for chartlite
 */

export { LineChart } from './charts/LineChart';

// Re-export types from core
export type {
  Theme,
  DataPoint,
  BaseChartConfig,
  LineChartConfig,
  BarChartConfig,
  AreaChartConfig,
  PieChartConfig,
} from '@chartlite/core';